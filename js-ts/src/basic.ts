let message: string = "hello world";

message = 'welcome broski.';

const age: number = 23;
const latittude: number = 1.43234;

const isActive: boolean = true;

const numbers: number[] = [1, 2, 3, 4, 5];
const names: string[] = ['saif', 'sajib', 'fahad'];
const flags: boolean[] = [true, true, false];


enum Role {
    ADMIN = 'ADMIN',
    MANAGER = 'MANAGER',
    DEVELOPER = 'DEVELOPER'
}

enum Status {
    ACTIVE = 'ACTIVE',
    INACTIVE = 'INACTIVE'
}